type Enrollment @key(fields: "id") @goModel(model: "github.com/lucasd-coder/classroom/internal/graphql/model.Enrollment") {
  id: ID!
  student: User!
  course: Course!
  canceledAt: Time
  createdAt: Time!
}